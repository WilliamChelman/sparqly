# common

Browser-safe primitives shared between `core`, `server`, and the web app.

## Building

Run `nx build common` to build the library.

## Running unit tests

Run `nx test common` to execute the unit tests via [Vitest](https://vitest.dev/).
